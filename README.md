# PolynomialSumCoefficients

main.cpp has one function other than the entry point: findSumFormula(int p)
This finds and prints the closed-form formula to the console for the sum from k = 1 to n of k^p
The default test program does so for p = 3, which can be changed by directly modifying main.cpp
